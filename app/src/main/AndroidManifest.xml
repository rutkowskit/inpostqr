<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="vrt.inpost.qr">

    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:theme="@style/AppTheme"
            android:name="vrt.inpost.qr.AppPrefsActivity"
            android:label="@string/labelPreferences"
            android:parentActivityName="vrt.inpost.qr.MainActivity"/>
        <activity
            android:name="vrt.inpost.qr.QrDisplayActivity"
            android:label="@string/qrCodeLabel"
            android:parentActivityName="vrt.inpost.qr.MainActivity"/>
        <activity
            android:name="vrt.inpost.qr.MainActivity"
            android:exported="true"
            android:label="@string/appLabel"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>